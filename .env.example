# Optional. CryptoPanic token if you want their aggregator on top of RSS.
# Leave empty for the default public RSS sources (no key required).
# https://cryptopanic.com/developers/api/
CRYPTOPANIC_AUTH_TOKEN=

# Optional. In-memory news cache, default 10 minutes.
# NEWS_CACHE_TTL_MS=600000
